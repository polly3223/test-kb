// import { authCalendar, createEvent } from '$lib/server/composioSendMail';
import type { PageServerLoad } from './$types';

export const load: PageServerLoad = async () => {
	// createEvent();
	// authCalendar();
};
